#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("ЭтоЗачет") И Параметры.ЭтоЗачет Тогда
		Заголовок = НСтр("ru = 'Список изменений оплаты'");
	Иначе
		Заголовок = НСтр("ru = 'Список изменений зачета предоплаты'");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти