#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если НЕ ПолучитьФункциональнуюОпцию("ИспользоватьСервис1СДоставка") = Истина Тогда
		ЭлементТипаГрузоперевозки = Элементы.ТипГрузоперевозки.СписокВыбора.НайтиПоЗначению(1);
		Элементы.ТипГрузоперевозки.СписокВыбора.Удалить(ЭлементТипаГрузоперевозки);
	КонецЕсли;
	
	Если НЕ ПолучитьФункциональнуюОпцию("ИспользоватьСервис1СКурьер") = Истина Тогда
		ЭлементТипаГрузоперевозки = Элементы.ТипГрузоперевозки.СписокВыбора.НайтиПоЗначению(1);
		Элементы.ТипГрузоперевозки.СписокВыбора.Удалить(ЭлементТипаГрузоперевозки);
	КонецЕсли;
	
	Если Элементы.ТипГрузоперевозки.СписокВыбора.Количество() = 0 Тогда
		Сообщение = НСтр("ru = 'Нет доступных систем доставки'");
		ОбщегоНазначения.СообщитьПользователю(Сообщение);
		Отказ = Истина;
	КонецЕсли;
	
	ТипГрузоперевозки = Элементы.ТипГрузоперевозки.СписокВыбора[0].Значение;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если Элементы.ТипГрузоперевозки.СписокВыбора.Количество() = 1 Тогда
		ТипГрузоперевозки = Элементы.ТипГрузоперевозки.СписокВыбора[0].Значение;
		ПараметрыОповещения = Новый Структура();
		ПараметрыОповещения.Вставить("ТипГрузоперевозки", ТипГрузоперевозки);
		Закрыть(ПараметрыОповещения);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КомандаВыбрать(Команда)
	
	ПараметрыОповещения = Новый Структура();
	ПараметрыОповещения.Вставить("ТипГрузоперевозки", ТипГрузоперевозки);
	Закрыть(ПараметрыОповещения);
	
КонецПроцедуры

#КонецОбласти
