
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Параметры.Свойство("Заголовок", Заголовок);
	Параметры.Свойство("ТекстСообщения", ТекстСообщения);
	Параметры.Свойство("ВидимостьБольшеНеПоказывать", ВидимостьБольшеНеПоказывать);
	Параметры.Свойство("ТекстЗаголовкаКнопкиОК", Элементы.ОК.Заголовок);
	Параметры.Свойство("ТекстЗаголовкаКнопкиОтмена", Элементы.Отмена.Заголовок);
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "БольшеНеПоказывать", "Видимость", ВидимостьБольшеНеПоказывать);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	Закрыть(Новый Структура("БольшеНеПоказывать, НажатаКнопкаОК", БольшеНеПоказывать, Истина));
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Закрыть(Новый Структура("БольшеНеПоказывать, НажатаКнопкаОК", БольшеНеПоказывать, Ложь));
	
КонецПроцедуры

#КонецОбласти