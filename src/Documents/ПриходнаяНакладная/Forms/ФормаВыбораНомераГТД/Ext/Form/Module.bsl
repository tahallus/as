
#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ЭтоПоступлениеИзОднойСтраныПриИзменении(Элемент)
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "СтранаПроисхождения", "Доступность", ЭтоПоступлениеИзОднойСтраны);
	
	Если НЕ ЭтоПоступлениеИзОднойСтраны Тогда
		
		СтранаПроисхождения = Неопределено;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	ПараметрыЗакрытия = Новый Структура;
	ПараметрыЗакрытия.Вставить("НомерГТД", НомерГТД);
	ПараметрыЗакрытия.Вставить("ЭтоПоступлениеИзОднойСтраны", ЭтоПоступлениеИзОднойСтраны);
	ПараметрыЗакрытия.Вставить("СтранаПроисхождения", СтранаПроисхождения);
	
	Закрыть(ПараметрыЗакрытия);
	
КонецПроцедуры

#КонецОбласти
