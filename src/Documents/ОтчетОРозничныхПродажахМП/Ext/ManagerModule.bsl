
#Область ОбработчикиСобытий

Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	Статус = ОбщегоНазначенияМПСервер.ЗначениеРеквизитаОбъекта(Данные.Ссылка, "Статус");
	Если ЗначениеЗаполнено(Статус) Тогда
		Если Статус = Перечисления.СтатусыКассовойСменыМП.Открыта Тогда
			СтатусПредставление = НСтр("ru=' (открыта)'")
		Иначе
			СтатусПредставление = НСтр("ru=' (закрыта)'")
		КонецЕсли;
	Иначе
		СтатусПредставление = НСтр("ru=' (не открыта)'");
	КонецЕсли;

	Представление = НСтр("en='Cashier shift No.';ru='Смена №'") + ОбщегоНазначенияМПКлиентСервер.ПолучитьПредставлениеНомера(Данные.Номер) + НСтр("ru=' от ';en=' of '") + Формат(Данные.Дата, "ДФ=dd.MM.yyyy") + СтатусПредставление;
	
КонецПроцедуры

#КонецОбласти
