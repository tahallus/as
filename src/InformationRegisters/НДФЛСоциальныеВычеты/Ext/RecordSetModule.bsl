#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, Замещение)

	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

	Для Каждого Запись Из ЭтотОбъект Цикл
		Если Запись.Применяется Тогда
			Запись.Представление = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр(
				"ru = 'Сумма расхода:%1'"), Запись.СуммаРасходов) + ?(ЗначениеЗаполнено(Запись.КодИФНС),
				СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = ', код ИФНС:%1'"), Запись.КодИФНС),
				"") + ?(ЗначениеЗаполнено(Запись.НомерУведомления),
				СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = ', номер уведомления:%1'"),
				Запись.НомерУведомления), "") + ?(ЗначениеЗаполнено(Запись.ДатаУведомления),
				СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = ', дата уведомления:%1'"),
				Запись.ДатаУведомления), "");
		Иначе
			Запись.Представление = НСтр("ru = 'Социальный вычет не применяется'");
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#Иначе
ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли