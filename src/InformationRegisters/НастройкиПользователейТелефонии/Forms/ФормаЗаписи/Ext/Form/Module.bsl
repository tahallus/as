
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если НЕ ЗначениеЗаполнено(Параметры.Ключ) И Параметры.Свойство("Пользователь") Тогда
		МенеджерЗаписи = РегистрыСведений.НастройкиПользователейТелефонии.СоздатьМенеджерЗаписи();
		МенеджерЗаписи.Пользователь = Параметры.Пользователь;
		МенеджерЗаписи.Прочитать();
		ЗначениеВРеквизитФормы(МенеджерЗаписи, "Запись");
	КонецЕсли;
	
	Если Параметры.Свойство("ТекущийЭлемент") Тогда
		Элемент = Элементы.Найти(Параметры.ТекущийЭлемент);
		Если Элемент <> Неопределено Тогда
			ТекущийЭлемент = Элемент;
		КонецЕсли;
	КонецЕсли;
	
	НастройкиФормы = ТелефонияПереопределяемый.НастройкиФормыПользователей();
	Элементы.ИсходящийНомер.Видимость = НастройкиФормы.ПоказыватьИсходящийНомер;
	
КонецПроцедуры

#КонецОбласти
