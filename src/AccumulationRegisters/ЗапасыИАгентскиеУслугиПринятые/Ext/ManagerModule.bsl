#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура создает пустую временную таблицу изменения движений.
//
Процедура СоздатьПустуюВременнуюТаблицуИзменение(ДополнительныеСвойства) Экспорт
	
	Если НЕ ДополнительныеСвойства.Свойство("ДляПроведения")
	 ИЛИ НЕ ДополнительныеСвойства.ДляПроведения.Свойство("СтруктураВременныеТаблицы") Тогда
		Возврат;
	КонецЕсли;
	
	СтруктураВременныеТаблицы = ДополнительныеСвойства.ДляПроведения.СтруктураВременныеТаблицы;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 0
	|	ЗапасыИАгентскиеУслугиПринятые.НомерСтроки КАК НомерСтроки,
	|	ЗапасыИАгентскиеУслугиПринятые.Организация КАК Организация,
	|	ЗапасыИАгентскиеУслугиПринятые.Номенклатура КАК Номенклатура,
	|	ЗапасыИАгентскиеУслугиПринятые.Характеристика КАК Характеристика,
	|	ЗапасыИАгентскиеУслугиПринятые.Партия КАК Партия,
	|	ЗапасыИАгентскиеУслугиПринятые.Контрагент КАК Контрагент,
	|	ЗапасыИАгентскиеУслугиПринятые.Договор КАК Договор,
	|	ЗапасыИАгентскиеУслугиПринятые.Заказ КАК Заказ,
	|	ЗапасыИАгентскиеУслугиПринятые.ТипПриемаПередачи КАК ТипПриемаПередачи,
	|	ЗапасыИАгентскиеУслугиПринятые.Количество КАК КоличествоПередЗаписью,
	|	ЗапасыИАгентскиеУслугиПринятые.Количество КАК КоличествоИзменение,
	|	ЗапасыИАгентскиеУслугиПринятые.Количество КАК КоличествоПриЗаписи,
	|	ЗапасыИАгентскиеУслугиПринятые.СуммаРасчетов КАК СуммаРасчетовПередЗаписью,
	|	ЗапасыИАгентскиеУслугиПринятые.СуммаРасчетов КАК СуммаРасчетовИзменение,
	|	ЗапасыИАгентскиеУслугиПринятые.СуммаРасчетов КАК СуммаРасчетовПриЗаписи
	|ПОМЕСТИТЬ ДвиженияЗапасыПринятыеИзменение
	|ИЗ
	|	РегистрНакопления.ЗапасыИАгентскиеУслугиПринятые КАК ЗапасыИАгентскиеУслугиПринятые");
	
	Запрос.МенеджерВременныхТаблиц = СтруктураВременныеТаблицы.МенеджерВременныхТаблиц;
	РезультатЗапроса = Запрос.Выполнить();
	
	СтруктураВременныеТаблицы.Вставить("ДвиженияЗапасыПринятыеИзменение", Ложь);
	
КонецПроцедуры // СоздатьПустуюВременнуюТаблицуИзменение()

#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.УправлениеДоступом

// Параметры:
//   Ограничение - см. УправлениеДоступомПереопределяемый.ПриЗаполненииОграниченияДоступа.Ограничение.
//
Процедура ПриЗаполненииОграниченияДоступа(Ограничение) Экспорт

	Ограничение.Текст =
	"РазрешитьЧтениеИзменение
	|ГДЕ
	|	ЗначениеРазрешено(Организация)
	|	И ЗначениеРазрешено(Контрагент)";

КонецПроцедуры

// Конец СтандартныеПодсистемы.УправлениеДоступом

#КонецОбласти

#КонецОбласти

#КонецЕсли