#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура создает пустую временную таблицу изменения движений.
//
Процедура СоздатьПустуюВременнуюТаблицуИзменение(ДополнительныеСвойства) Экспорт
	
	Если НЕ ДополнительныеСвойства.Свойство("ДляПроведения")
	 ИЛИ НЕ ДополнительныеСвойства.ДляПроведения.Свойство("СтруктураВременныеТаблицы") Тогда
		Возврат;
	КонецЕсли;
	
	СтруктураВременныеТаблицы = ДополнительныеСвойства.ДляПроведения.СтруктураВременныеТаблицы;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 0
	|	БонусныеБаллы.НомерСтроки КАК НомерСтроки,
	|	БонусныеБаллы.БонуснаяКарта КАК БонуснаяКарта,
	|	БонусныеБаллы.Начислено КАК НачисленоПередЗаписью,
	|	БонусныеБаллы.Начислено КАК НачисленоИзменение,
	|	БонусныеБаллы.Начислено КАК НачисленоПриЗаписи,
	|	БонусныеБаллы.КСписанию КАК КСписаниюПередЗаписью,
	|	БонусныеБаллы.КСписанию КАК КСписаниюИзменение,
	|	БонусныеБаллы.КСписанию КАК КСписаниюПриЗаписи
	|ПОМЕСТИТЬ ДвиженияБонусныеБаллыИзменение
	|ИЗ
	|	РегистрНакопления.БонусныеБаллы КАК БонусныеБаллы");
	
	Запрос.МенеджерВременныхТаблиц = СтруктураВременныеТаблицы.МенеджерВременныхТаблиц;
	РезультатЗапроса = Запрос.Выполнить();
	
	СтруктураВременныеТаблицы.Вставить("ДвиженияБонусныеБаллыИзменение", Ложь);
	
КонецПроцедуры // СоздатьПустуюВременнуюТаблицуИзменение()

#КонецОбласти

#КонецЕсли