#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Некоторые колонки списка не имеют значения при выборе шаблона чека
	ТипШаблона = Неопределено;
	Параметры.Отбор.Свойство("ТипШаблона", ТипШаблона);
	Если ТипШаблона = Перечисления.ТипыШаблонов.ЧекККМ Тогда
		
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
			Элементы,
			"ТипМакета",
			"Видимость",
			Ложь);
			
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
			Элементы,
			"РазмерМакета",
			"Видимость",
			Ложь);
			
	КонецЕсли;
	
КонецПроцедуры
 
#КонецОбласти