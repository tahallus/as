#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	ВариантНастройкиИспользуется = УправлениеМобильнымиПриложениями.ПроверитьВариантНастройкиМобильногоПриложения(
		Ссылка, ВариантНастройки);
	
	Если ВариантНастройкиИспользуется Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Вариант настройки ""%1"" уже используется'"), ВариантНастройки);
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения, ЭтотОбъект,,, Отказ);
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#КонецЕсли