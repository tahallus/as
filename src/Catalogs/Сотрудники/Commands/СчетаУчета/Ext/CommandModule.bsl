
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ОткрытьФорму(
	"Справочник.Сотрудники.Форма.ФормаРедактированияСчетовУчета",
	СтруктураПараметров(ПараметрКоманды),
	ПараметрыВыполненияКоманды.Источник,
	ПараметрыВыполненияКоманды.Уникальность,
	ПараметрыВыполненияКоманды.Окно,
	ПараметрыВыполненияКоманды.НавигационнаяСсылка);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция СтруктураПараметров(ПараметрКоманды)
	
	Результат = Новый Структура;
	Результат.Вставить("СчетРасчетовСПерсоналом", ПараметрКоманды.СчетРасчетовСПерсоналом);
	Результат.Вставить("СчетРасчетовСПодотчетниками", ПараметрКоманды.СчетРасчетовСПодотчетниками);
	Результат.Вставить("СчетРасчетовПоПерерасходу", ПараметрКоманды.СчетРасчетовПоПерерасходу);
	Результат.Вставить("Ссылка", ПараметрКоманды.Ссылка);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти
