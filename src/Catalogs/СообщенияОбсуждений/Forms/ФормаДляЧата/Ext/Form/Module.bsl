#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ПервоеСлово = СтрНайти(Объект.Наименование, " ");
	// Обрезаем двоеточие, для заголовка оно не используется
	ЭтотОбъект.Заголовок = Лев(Объект.Наименование, ПервоеСлово-2);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КопироватьВБуфер(Команда)
	
	Документ = Элементы.БуферОбмена.Документ;
	Если Документ <> Неопределено Тогда
		Попытка
			Документ.parentWindow.ClipboardData.SetData("Text", Объект.ПолныйТекст);
			ПоказатьОповещениеПользователя(НСтр("ru = 'Текст скопирован'"), , НСтр(
				"ru = 'Текст сообщения скопирован в буфер обмена'"));
		Исключение
			ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Возникла ошибка при копировании в буфер обмена.
															 |Воспользуйтесь комбинацией клавиш Ctrl+C.'"));
		КонецПопытки;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
