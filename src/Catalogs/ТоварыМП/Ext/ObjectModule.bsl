#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Возвращает цену продажи.
//
// Возвращаемое значение:
//  Число
//
Функция ПолучитьЦенуПродажи() Экспорт
	
	Возврат РегистрыСведений.ЦеныТоваровМП.ПолучитьЦенуТовара(Ссылка);
	
КонецФункции // ПолучитьЦенуПродажи()

// Возвращает цену поставщиков.
//
// Возвращаемое значение:
//  Число
//
Функция ПолучитьЦенуПоставщиков() Экспорт
	
	Возврат РегистрыСведений.ЦеныПоставщиковМП.ПолучитьЦенуТовара(, Ссылка);
	
КонецФункции // ПолучитьЦенуПоставщиков()

// Возвращает остаток товара.
//
// Возвращаемое значение:
//  Число
//
Функция ПолучитьОстатокТовара() Экспорт
	
	Возврат РегистрыНакопления.ОстаткиТоваровМП.ПолучитьКоличествоОстаткаТовара(Ссылка);
	
КонецФункции // ПолучитьОстатокТовара()

Процедура ПриКопировании(ОбъектКопирования)
	
	ТекущийОстаток = 0;
	
КонецПроцедуры

#КонецОбласти

#Иначе
ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли