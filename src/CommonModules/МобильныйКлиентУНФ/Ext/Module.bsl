#Область ПрограммныйИнтерфейс

// Функциональность временно недоступна.
// 
// Параметры:
//  Отказ - Булево - Отказ
// 
// Возвращаемое значение:
//  Булево - Функциональность временно недоступна
Функция ФункциональностьВременноНедоступна(Отказ) Экспорт

	Если Не ОбщегоНазначения.ЭтоМобильныйКлиент() Тогда
		Возврат Ложь;
	КонецЕсли;

	ОбщегоНазначения.СообщитьПользователю(
	НСтр("ru = 'В настоящий момент данная функциональность доступна только в тонком и веб-клиентах.'"), , , , Отказ);

	Возврат Истина;

КонецФункции

// Настраивает форму объекта для корректного отображения в мобильном клиенте.
//
// Параметры:
//  ЭлементыФормы	 - ВсеЭлементыФормы
//  Таблицы			 - Строка - Имена элементов-таблицы
//
Процедура НастроитьФормуОбъектаМобильныйКлиент(ЭлементыФормы, Таблицы = "Запасы") Экспорт

	Если Не ОбщегоНазначения.ЭтоМобильныйКлиент() Тогда
		Возврат;
	КонецЕсли;
	
	// Для нестандартных кнопок на форме, например, когда выполняется сбор статистики использования.
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(ЭлементыФормы, "ФормаПровестиИЗакрыть", "Отображение",
		ОтображениеКнопки.Картинка);
	
	// Если у группы стоит сквозное выравнивание = Авто, которое нужно в тонком клиенте, то некоторые заголовки полей имеет
	// избыточную ширину.
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(ЭлементыФормы, "ГруппаНомерДата",
		"СквозноеВыравнивание", СквозноеВыравнивание.НеИспользовать);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(ЭлементыФормы, "НомерДата", "СквозноеВыравнивание",
		СквозноеВыравнивание.НеИспользовать);

КонецПроцедуры

// Настраивает вспомогательную форму для корректного отображения в мобильном клиенте.
//
// Параметры:
//  Форма	 - ФормаКлиентскогоПриложения, для которой следует настроить отображение.
//
Процедура НастроитьВспомогательнуюФормуМобильныйКлиент(Форма) Экспорт

	Если Не ОбщегоНазначения.ЭтоМобильныйКлиент() Тогда
		Возврат;
	КонецЕсли;

	Если Форма.ПоложениеКоманднойПанели = ПоложениеКоманднойПанелиФормы.Низ Тогда
		Форма.ПоложениеКоманднойПанели = ПоложениеКоманднойПанелиФормы.Авто;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти