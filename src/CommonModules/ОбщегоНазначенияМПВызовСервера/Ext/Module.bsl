#Область СлужебныйПрограммныйИнтерфейс

// Получает значение любой константы.
//
Функция ПолучитьЗначениеКонстанты(ИмяКонстанты) Экспорт
	
	Возврат Константы[ИмяКонстанты].Получить();
	
КонецФункции

// Устанавливает значение любой константы.
//
Процедура УстановитьЗначениеКонстанты(ИмяКонстанты, Значение) Экспорт
	
	Константы[ИмяКонстанты].Установить(Значение);
	
КонецПроцедуры

// Возвращает принадлежность статьи взаиморасчетам с контрагентами.
//
Функция СтатьяЭтоРасчетыСКонтрагентами(Статья) Экспорт
	
	ЭтоРасчетыСКонтрагентами = Ложь;
	
	Если ОбщегоНазначенияМПСервер.ЕстьПредопределенныйВДанных("СтатьиМП", "ОплатаОтПокупателя")
		И Статья = Справочники.СтатьиМП.ОплатаОтПокупателя Тогда
		ЭтоРасчетыСКонтрагентами = Истина;
	КонецЕсли;
	Если ОбщегоНазначенияМПСервер.ЕстьПредопределенныйВДанных("СтатьиМП", "ОплатаПоставщику")
		И Статья = Справочники.СтатьиМП.ОплатаПоставщику Тогда
		ЭтоРасчетыСКонтрагентами = Истина;
	КонецЕсли;
	
	Возврат ЭтоРасчетыСКонтрагентами;
	
КонецФункции

// Возвращает значение параметра сеанса "ВерсияОС".
//
// Возвращаемое значение:
//   Строка
//
Функция ВерсияОС() Экспорт
	
	//Возврат ПараметрыСеанса.ВерсияОС;
	Возврат "Windows"
	
КонецФункции // ВерсияОС()

// Кодирует строку.
//
Функция Кодировать(Строка) Экспорт
	
	Возврат КодироватьСтроку(Строка, СпособКодированияСтроки.КодировкаURL);
	
КонецФункции

// Возвращает значение параметра сеанса "Смартфон".
//
// Возвращаемое значение:
//   Булево
//
Функция ЭтоСмартфон() Экспорт
	
	//Возврат ПараметрыСеанса.Смартфон;
	Возврат Ложь;
	
КонецФункции // ЭтоСмартфон()

Функция ТекущаяВерсия() Экспорт

	СинхронизацияМПУНФ.ПолучитьТекущуюВерсиюПриложения();

КонецФункции // ()

Функция ЭтоПланшет1С () Экспорт

	Возврат Ложь;

КонецФункции // ()


#КонецОбласти