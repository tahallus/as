
#Область ОбработчикиСобытийФормы
&НаСервере
// Процедура - обработчик события ПриСозданииНаСервере
//
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Параметры.Свойство("Заголовок", Заголовок);
	Параметры.Свойство("ТекстСообщения", ТекстСообщения);
	Элементы.ТекстСообщения.Высота = Окр(СтрДлина(ТекстСообщения)/50)+1;
	Высота = 5+Элементы.ТекстСообщения.Высота;
	Параметры.Свойство("ВидимостьБольшеНеПоказывать", ВидимостьБольшеНеПоказывать);
	Если Параметры.Свойство("ЗаголовокКнопкиОк") Тогда
		Элементы.ОК.Заголовок = Параметры.ЗаголовокКнопкиОк;
	КонецЕсли; 
	Если Параметры.Свойство("ЗаголовокКнопкиОтмена") Тогда
		Элементы.Отмена.Заголовок = Параметры.ЗаголовокКнопкиОтмена;
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "БольшеНеПоказывать", "Видимость", ВидимостьБольшеНеПоказывать);
	
	КлючСохраненияПоложенияОкна = "ОбщаяФорма.ФормаПредупреждения.Высота"+Высота;
	
	МобильныйКлиентУНФ.НастроитьВспомогательнуюФормуМобильныйКлиент(ЭтотОбъект);
	
КонецПроцедуры // ПриСозданииНаСервере()

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
// Процедура - обработчик команды ОК
//
Процедура ОК(Команда)
	
	СтруктураВозврата = Новый Структура;
	СтруктураВозврата.Вставить("БольшеНеПоказывать", БольшеНеПоказывать);
	СтруктураВозврата.Вставить("КодВозврата", КодВозвратаДиалога.ОК);
	Закрыть(СтруктураВозврата);
	
КонецПроцедуры // ОК()

&НаКлиенте
Процедура Отмена(Команда)
	
	СтруктураВозврата = Новый Структура;
	СтруктураВозврата.Вставить("БольшеНеПоказывать", БольшеНеПоказывать);
	СтруктураВозврата.Вставить("КодВозврата", КодВозвратаДиалога.Отмена);
	Закрыть(СтруктураВозврата);
	
КонецПроцедуры

#КонецОбласти
