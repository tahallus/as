#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

Функция Сравнить(Значение1, Значение2) Экспорт
	
	ВесЗначений = Новый Соответствие;
	ВесЗначений.Вставить(Перечисления.СтатусОплаты.НеУчитывается, 0);
	ВесЗначений.Вставить(Перечисления.СтатусОплаты.Отсутствует, 0);
	ВесЗначений.Вставить(Перечисления.СтатусОплаты.Частичная,   1);
	ВесЗначений.Вставить(Перечисления.СтатусОплаты.Полная,      2);
	
	Если ВесЗначений.Получить(Значение1) = Неопределено ИЛИ ВесЗначений.Получить(Значение2) = Неопределено Тогда
		ВызватьИсключение СтрШаблон(НСтр("ru='Не определена операция сравнения для переданных значений: ""%1"", ""%2""'"), Значение1, Значение2);
	КонецЕсли;
	
	Возврат ВесЗначений.Получить(Значение1) - ВесЗначений.Получить(Значение2);
	
КонецФункции

Функция НовыйСтатусОплаты(ПроцентОплаты) Экспорт
	
	Если ПроцентОплаты = 0 Тогда
		Возврат Перечисления.СтатусОплаты.Отсутствует;
	ИначеЕсли ПроцентОплаты > 0 И ПроцентОплаты < 100 Тогда
		Возврат Перечисления.СтатусОплаты.Частичная;
	ИначеЕсли ПроцентОплаты = 100 Тогда
		Возврат Перечисления.СтатусОплаты.Полная;
	КонецЕсли;
	
КонецФункции

#КонецОбласти


#КонецЕсли