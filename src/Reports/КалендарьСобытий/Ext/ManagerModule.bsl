
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.ВариантыОтчетов

// Вызывается при работе в модели сервиса для получения сведений о предопределенных вариантах отчета.
//
// Возвращаемое значение:
//  Массив из Структура:
//    * Имя           - Строка - имя варианта отчета; например, "Основной";
//    * Представление - Строка - имя варианта отчета; например, НСтр("ru = 'Динамика изменений файлов'").
//
Функция ВариантыНастроек() Экспорт 
	
	Результат = Новый Массив;
	Результат.Добавить(Новый Структура("Имя, Представление", "КалендарьСобытий", 
		НСтр("ru = 'Календарь событий'")));
	Результат.Добавить(Новый Структура("Имя, Представление", "КалендарьСобытийПоОтветственным", 
		НСтр("ru = 'Календарь событий по ответственным'")));
	Результат.Добавить(Новый Структура("Имя, Представление", "КалендарьСобытийПоКонтактам", 
		НСтр("ru = 'Календарь событий по контактам'")));
	Результат.Добавить(Новый Структура("Имя, Представление", "КалендарьСобытийПоТемам", 
		НСтр("ru = 'Календарь событий по темам'")));
	Возврат Результат;
	
КонецФункции

// Параметры:
//   Настройки - см. ВариантыОтчетовПереопределяемый.НастроитьВариантыОтчетов.Настройки.
//   НастройкиОтчета - см. ВариантыОтчетов.ОписаниеОтчета.
//
Процедура НастроитьВариантыОтчета(Настройки, НастройкиОтчета) Экспорт
	
	ВариантыОтчетов.УстановитьРежимВыводаВПанеляхОтчетов(Настройки, НастройкиОтчета, Ложь);
	НастройкиОтчета.ОпределитьНастройкиФормы = Истина;
	
	Вариант = ВариантыОтчетов.ОписаниеВарианта(Настройки, Метаданные.Отчеты.КалендарьСобытий, "КалендарьСобытий");
	Вариант.Описание = НСтр("ru = 'Список всех событий с отбором по периоду без группировок.'");
	Вариант.Размещение.Вставить(Метаданные.Подсистемы.CRM.Подсистемы.События, "Важный");
	
	Вариант = ВариантыОтчетов.ОписаниеВарианта(Настройки, Метаданные.Отчеты.КалендарьСобытий, "КалендарьСобытийПоОтветственным");
	Вариант.Описание = НСтр("ru = 'Список всех событий с отбором по периоду с группировкой по ответственным лицам.'");
	
	Вариант = ВариантыОтчетов.ОписаниеВарианта(Настройки, Метаданные.Отчеты.КалендарьСобытий, "КалендарьСобытийПоКонтактам");
	Вариант.Описание = НСтр("ru = 'Список всех событий с отбором по периоду с группировкой по контактам.'");
	
	Вариант = ВариантыОтчетов.ОписаниеВарианта(Настройки, Метаданные.Отчеты.КалендарьСобытий, "КалендарьСобытийПоТемам");
	Вариант.Описание = НСтр("ru = 'Список всех событий с отбором по периоду с группировкой по темам.'");
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.ВариантыОтчетов

#КонецОбласти

#КонецОбласти

#КонецЕсли